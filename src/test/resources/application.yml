spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:latest:///test-database
  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
